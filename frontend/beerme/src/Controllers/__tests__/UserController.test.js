// import UserController from "../UserController";

test("invalid email with a valid passowrd to login", () => {
    // let value = UserController.login("wrong-email", "password123");
    // expect(value).toBeFalsy();
    expect(1).toBe(1);
});
